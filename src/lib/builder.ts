import { Builder } from '@builder.io/sdk';

// Create a new builder instance
export const builder = new Builder({
  apiKey: '498d9c2c0b6f474c9bc9e2778b114199',
  userAttributes: {
    urlPath: '/'
  }
});
